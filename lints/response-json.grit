`new Response($args)` where {
    not {
        or {
            $args <: contains `"Content-Type"`,
        },
    },
    register_diagnostic(
        span = $args,
        message = "Prefer `Response.json`, for structured data, over `new Response()`"
    )
}
