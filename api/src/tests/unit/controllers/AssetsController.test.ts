describe("Unit Tests for Asset Controller", () => {

  it("Creates an Asset in the database and returns true", () => {});

  it("Unable to create an Asset in the database and returns false", () => {});

  it("Returns all 2 Assets currently stored in the database", () => {});

  it("Returns an empty Array as no Assets are stored in the database", () => {});

  it("Returns Asset data from provided Asset Number", () => {});

  it("Returns null as no Asset found on provided Asset Id", () => {});

  it("Updates Asset in database and returns true", () => {});

  it("Unable to update Asset in database and returns false", () => {});

  it("Successfully deletes Asset on provided Id and returns true", () => {});

  it("Unable to delete Asset on provided Id and returns false", () => {});

});
